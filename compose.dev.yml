services:
  frontendmentor-playground-service:
    container_name: frontendmentor-playground-container
    build:
      context: .
      dockerfile: ./Dockerfile.dev
      args:
        USER_NAME: ${USER_NAME:-node}
        USER_GROUP_NAME: ${USER_GROUP_NAME:-workspace}
        USER_UID: ${USER_UID:-1000}
        USER_GID: ${USER_GID:-1000}
    tty: true
    volumes:
      - .:/workspace:cached
      -  frontendmentor-playground-service-node-modules:/workspace/node_modules/

    ports:
      - 5555:5555

  # mongo service example configuration
  #
  # mongo-playground:
  #   container_name: mongo-workspace
  #   image: mongo:4.4.6
  #   ports:
  #     - 27017:27017

  # postgres service example configuration
  # db:
  #   image: postgres:latest
  #   restart: unless-stopped
  #   volumes:
  #     - postgres-data:/var/lib/postgresql/data
  #   environment:
  #     POSTGRES_USER: postgres
  #     POSTGRES_DB: postgres
  #     POSTGRES_PASSWORD: postgres
  #

volumes:
  frontendmentor-playground-service-node-modules:
